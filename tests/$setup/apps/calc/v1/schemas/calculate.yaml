output:
  type: object
  properties:
    operation:
      type: string
      enum:
        - add
        - subtract
        - multiply
        - divide
    values:
      type: string
      description: stringified JSON array of numbers
job:
  type: object
  properties:
    result:
      type: number
