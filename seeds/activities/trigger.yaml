title: Review Order
type: trigger
settings:
  schema:
    $ref: '../schemas/trigger.yaml#/settings'
  maps:
    $ref: '../maps/trigger.yaml#/settings'
input:
  schema:
    $ref: '../schemas/trigger.yaml#/input'
output:
  schema:
    $ref: '../schemas/trigger.yaml#/output'
return:
  schema:
    $ref: '../schemas/trigger.yaml#/return'
errors:
  schema:
    $ref: '../schemas/trigger.yaml#/errors'
stats:
  key: "{activity1.input.data.object_type}"
  id: "{activity1.input.data.id}"
  measures:
    - measure: avg
      target: "{activity1.input.data.price}"
    - measure: count
      target: "{activity1.input.data.price}"